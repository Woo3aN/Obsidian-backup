---
title: "【720P cc字幕】数字图像处理 Guillermo Sapiro 杜克大学Image and Video Processing 第一周"
url: "https://www.bilibili.com/video/BV12E411n76B/?p=8"
bvid: "BV12E411n76B"
cid: "157964115"
author: "筷_拨"
upload_date: "2020-02-28"
subtitle_lang: "中文（中国）"
created: "2026-07-31"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=92367700&bvid=BV12E411n76B&cid=157964115&page=8&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

课程名称:Image and video processing: From Mars to Hollywood with a stop at the hospital 
教授:Guillermo Sapiro of Duke university
教材:冈萨雷斯第三版
来源:https://www.coursera.org/learn/image-processing/lecture/CCSy7/0-welcome-and-start-here

## 字幕

`00:00` 利用上一个视频讲到的离散二维数组
`00:06` 计算机可以这样描绘出图像
`00:13` 也就是我们从这张连续的图片 得到了这个二维离散数组
`00:18` 现在我们就可以对这些图片进行处理了
`00:21` 现在图像在我们的电脑中 它们可被看成像素的二维数组
`00:25` 我们就能够基于此进行各种各样的处理
`00:30` 就如我们前几节课视频看到的那样 在接下来几周里
`00:35` 我们将会学到一些非常复杂的操作 我们先从一些简单的例子开始
`00:39` 它们虽然简单但却非常有用
`00:44` 能够让我们知道如何真正地处理这些图片
`00:47` 你也可以用自己的电脑 来实践我一会给大家演示的例子
`00:51` 如果你有MATLAB这样的软件 这只需要几分钟时间或者更短
`00:56` MATLAB 是一种常被用来处理二维数组的软件
`01:01` 当有了一个二维矩阵 我们可以就开始
`01:06` 探讨邻域处理 要记住 我们有一个二维数组
`01:10` 现在我们要在像素层面探讨 在这里我画了一个像素点
`01:15` 我要问哪些像素和这个像素相邻
`01:20` 这里介绍图像处理中的两种邻域
`01:25` 一种称之为 “4邻域”
`01:29` 只包含画出的这四个点 也就是说你只能
`01:34` 去找上下左右的四个像素 所以这就是 4领域
`01:38` 另一种常见的类型就是 “8邻域”
`01:44` 你或许已经猜出来是怎么回事了
`01:48` 如果是这个像素点 就要取环绕着它的所有相邻像素
`01:53` 你可以和刚才一样 看到上下左右的点
`01:59` 你也可以看到像左上角 这样的对角邻像素
`02:04` 举个例子 看看这张图
`02:10` 这个像素的 4邻域像素中
`02:14` 不包括这个点 因此 这两个 1 不相邻
`02:19` 这是因为4邻域关系 并不包括对角线位置的点
`02:23` 而在8邻域像素关系中 它们就是相邻像素了
`02:28` 当然 这个概念也可以迁移用于像素组中
`02:32` 上面这个像素组和下面的这个像素组存在8邻域的关系
`02:38` 因为有一个 1 在这儿 而另一个 1 在它的对角线上
`02:41` 这时可以考虑对角线上的点
`02:44` 现在 如果我们考虑的是4邻域的话 我可以说
`02:50` 上面这一组 1 与下面这一组 1 是不相邻的
`02:54` 因为从这个 1 出发 我们只能向下找
`02:57` 在下面这个组中 我看不到和它相邻的“1”
`03:01` 在这个 1 的右边 也没有 1
`03:04` 因此 我们能够进行什么样的操作取决于在这个离散空间中
`03:10` 是如何定义邻域的 我们将会看到这很重要
`03:15` 这种邻域 在现代图像处理中最为常见
`03:19` 但是了解这种领域也很重要
`03:22` 尤其是在我们进行图像分割或者类似的操作时
`03:26` 再比如说 我们今后会谈到
`03:31` 我们会讲解 三维图像
`03:34` 不是那种平面上的有灰度值的图像
`03:37` 就像这样 每个像素都有一个灰度值的这个平面
`03:40` 但是我们将要说到的体积图像 位于其空间内的每个点
`03:45` 称之为 体素 (voxel) 都有灰度值
`03:48` 邻域关系对于这种图像而言非常重要
`03:53` 比如说 在医学图像中 判断这个器官和另一个器官是否相邻
`03:56` 我们就需要界定邻域关系
`04:00` 在图像分割的时候 也是如此
`04:04` 邻域如此重要 但现在我们仅就二维数组中进行讨论
`04:09` 我们对在二维数组 进行一些简单的操作
`04:14` 比如说我们可以叠加它们 记住 每一个点 例如这张图
`04:19` 每个点都有相应的值 因此 我可以把这张图像叠加到第二张上
`04:25` 所以我可以把图像一
`04:29` 加到图像二上 这就意味着 我特意在这画一个小图像
`04:36` 图像为三行三列
`04:40` 我取这个点的值与另外这个点的值相加
`04:44` 我假设这个值是7 另一个的值是10 那么结果就是17
`04:50` 下一个像素我也这么处理
`04:53` 这种图像相加很简单
`04:56` 是一种简单却又很重要的处理方法
`05:00` 我们看看这个操作
`05:03` 这里有好几张NASA拍摄的同一个星系的图像
`05:07` 他们拍摄 再拍摄  重复多次 得到越来越多的图像
`05:11` 这张图像噪点很多 这些图像排列在一起
`05:15` 他们不移动相机 什么都不变
`05:18` 然后一张接一张拍摄 再叠加起来
`05:21` 随着他们对图像的叠加 图像开始变得越来越清晰
`05:26` 因此这里 他们叠加了五张
`05:30` 然后又叠加十张 直到叠加了100张
`05:35` 也就是他们把 I1+I2 的操作进行了100次
`05:41` 就是把一百张的星系图片 叠加到一起
`05:46` 使得得到的图像开始变得越来越清晰 这是因为图像有噪声
`05:50` 这些图像中的场景固定不变
`05:54` 这些噪声却不断变化 当你把图片都叠加起来
`06:00` 相当于叠加了图片中拍摄对象的像素值 而噪声又是随机的
`06:05` 这样噪声就显得微不足道了 当我们讨论如何处理空间噪声
`06:10` 以及讨论如何增强图像的时候 我们将非常详细地讨论这个话题
`06:14` 那么基本思路就是 我们可以简单地将一些二维数组相加
`06:20` 从而完成一些有趣的处理 我们还可以做其他的事情
`06:25` 我们还可以对图像进行掩模处理或者相减运算
`06:30` 我们可以看到这幅图像 还有这幅图像
`06:38` 这是同一场景在不同情况下拍摄的两张图像
`06:42` 接下来 我们找出了它们之间的差别
`06:46` 你看 这个凸显出来的不同之处
`06:49` 可能对于医生来讲非常非常重要 而这是增强以后的图像
`06:55` 我们往后会讲到如何增强图像
`07:00` 例如 通过改变灰度值的分布使其更加清晰
`07:03` 那么这里 上面两幅图我们标记为 I1、I2 然后进行了 I1-I2
`07:14` 如果不关心正负的话 我们有时候也会取绝对值
`07:19` 有时 我们只是移走所有负数值的像素
`07:22` 这样就把负值都变成了 0
`07:25` 这再一次证明了这种简单的操作也具有重要意义
`07:31` 有时 我们也可以对图像进行逻辑运算
`07:35` 我们可以对图像A和图像B 做A并B的运算
`07:42` 让我们假设这部分 (A部分) 的值都是255
`07:46` 其余部分都是0 B部分的值也全部是255
`07:50` 然后得到并集 这个图我们得到了交集
`07:53` 即 在A和B中 值为255或者白色值的所有像素
`07:58` 同理 想一想用你的电脑你可以对二维数组进行哪些运算
`08:04` 你也可以对图像进行相同操作 现在我们知道了
`08:10` 用计算机操作二维数组是多么重要 我们还能对一幅图像进行反相
`08:16` 基本思想是 这部分特别黑的地方是0
`08:22` 最亮的这个点是255
`08:28` 然后我们可以进行反相 将0变成255
`08:33` 将255变成0 在坐标里表示就是这样
`08:38` 假设横轴是输入 纵轴是输出
`08:46` 画出来就是这样的 0对应成255 255对应成0
`08:51` 我们对图像反相操作 利用这个我们可以发现一些有趣的事情
`08:57` 也许反相后的图像会比原来的图像
`09:02` 更容易从中观察到一些对象
`09:06` 总结一下 数组的这些简单操作可通过计算机运用于图像处理之中
`09:12` 我们也能做不同的空间操作
`09:16` 比如图像叠加 我用一张图片
`09:19` 再拿另一张图片 然后相加
`09:21` 或是图像反相之类 但是我们还能做一些更复杂的事情
`09:25` 事实上我们还可以把图像中一个像素替换为的与之相邻像素的平均值
`09:30` 注意 需要先定义相邻像素的类型
`09:34` 好的？
`09:35` 因此 我们可以设为 8邻域 我们之前讲到过这个
`09:41` 我可以把这个像素替换为所有像素的平均值 也包括它自己
`09:47` 因而我们只要把所有像素的值相加
`09:51` 然后再除以9 把得到的数值赋给这个像素
`09:56` 这个是原图
`10:00` 我们把每一个像素点都做了这样的处理然后是这样
`10:04` 所以对于这个像素 我把它标记在这里
`10:08` 我们找到了它的 8邻域 然后把这个像素替换为
`10:14` 周围所有像素的平均值
`10:18` 然后对于下一个像素 我们也这样做
`10:21` 这里发生了交叠 我用另外一个颜色标出来
`10:29` 因此 这个像素会用到这个 这个 这个
`10:34` 这个 这个 这个 这个 还有这个 蓝色的点则用到了这个
`10:42` 这个和这个 这是它自己的 8邻域
`10:49` 之所以会交叠 是因为我们一次变换一个像素
`10:54` 然后我们对图像中每个像素都是这样一个一个处理
`10:58` 极力推荐你做一下这个小练习
`11:00` 这就是我们得到的结果 我们移除了噪点
`11:05` 但却得到一张很模糊的图像 我将会说明为什么会这样
`11:08` 在一些情况下 这是一种的很有用的图像处理应用
`11:12` 我们将会了解这是怎么实现的
`11:15` 我们所选取的相邻像素 事实上并非那么简单
`11:20` 当像素间变化比较大时 我们不会用到另一边的像素
`11:25` 因此 最终可能只用了 6个像素进行平均
`11:28` 而不是通常 9个像素的平均
`11:32` 这是一种非常简单的操作 每个像素被替换为一种线性组合
`11:37` 在这里 就是指替换为周围像素的平均值
`11:42` 另一种图像处理方式是图像变换
`11:46` 我们可以做各种不同的图像变换
`11:50` 这个是我们没有变化的原始图像
`11:54` 我们可以对它进行缩放 比如使其变大
`11:57` 我们也可以让图像旋转、移动、平移等等
`12:01` 我们可以用数组进行平移变换
`12:05` 我们还可以用数组进行旋转变换和缩放变换
`12:10` 它们都是计算机中的二维数组 因此我们可以进行任意的这些操作
`12:16` 这就是操作的效果 如果我们要旋转图像
`12:20` 我们要进行旋转变换 我们把 T 所在的图像整个旋转
`12:26` 从而看上去这个 T 被旋转了一些角度
`12:29` 放大边缘看一下 这再一次说明了离散化的问题
`12:34` 当我画一条直线的时候
`12:40` 放在这个离散化的图像中 如果是这么竖直画的话
`12:46` 就不会有问题 但是 当一条直线这样画
`12:52` 不位于中间 而是穿过对角线方向
`12:55` 就会产生像我们在这看到的这种不完整的效果
`12:59` 像素点越多的话
`13:03` 我们就越难察觉这种问题 这里我们只讨论旋转的可能后果
`13:08` 对图像进行不同操作的可能后果
`13:14` 这是一个完整的例子 我希望你能看看它
`13:20` 这是我的原始图像 这是旋转后的图像
`13:23` 我们也可以把它转回来 看上去还算不错
`13:29` 但还是有些不同 这是两幅图像的不同之处
`13:33` 如果要写出来这个过程
`13:38` 我有一张图 I 然后我旋转它 R(I)
`13:43` 这个的结果 就得到了这个图
`13:50` 再朝相反方向旋转它 就能得到这幅图
`13:57` 然后两张图相减 也就是这个部分减去 I
`14:02` 这是 I  这是旋转后的图像 这是逆旋转的图像
`14:07` 这个是相减后的图像 两者不同的原因就是
`14:12` 因为采样和离散化 我们在之前的视频中讨论过
`14:18` 还有一点 我们可以实现的
`14:22` 将在下一周讲述关于图像压缩的话题时讨论
`14:28` 我们可以对图像进行变换
`14:31` 你们在数字信号处理的课程中已经知道了 有一种变换是
`14:36` 傅里叶变换 (Fourier transform) 我们可以对一幅图像进行傅里叶变换
`14:41` 而在图像压缩中非常重要的一点是
`14:45` 我们不做傅里叶变换 我们要进行一种
`14:48` 叫做离散余弦变换 (Discrete Cosine Transform) 的变换
`14:52` 这是一幅图像 我们对它进行傅里叶变换
`15:00` 这是傅里叶变换后的图像
`15:03` 你现在所看到的是噪声能够在傅里叶域中滤除掉
`15:09` 我不在这讨论关于傅里叶的信号处理 (signal processing) 的背景知识
`15:15` 而且我们也不需要了解很多 我将会在今后每周
`15:21` 的课程中讲到那周需要的背景知识 实际上我们也不会用到太多的傅里叶
`15:25` 如果你不具备这些知识 那么不用傅里叶变换
`15:28` 我们也可以完成许多的图像处理
`15:32` 因此 你也不必去上数字信号处理的课程 别担心
`15:36` 除了这一页幻灯片 我们不用傅里叶就可以完成所有事情
`15:40` 所以 不用担心 但如果你们熟悉傅里叶的话
`15:44` 你就能看到滤波的效果 比如说 这个是滤波器
`15:49` 这是处理后的结果 因此 我完成了变换
`15:51` 经过滤波和逆变换 得到了一张非常清晰的图像
`15:55` 再重复一遍 如果你们熟悉基本的关于傅里叶变换的信号处理知识
`15:59` 这里也只会用到傅里叶滤波器
`16:04` 如果你们对此不太熟悉的话 你可以理解为我把这幅图像
`16:09` 这个二维数组变换为另一个不同的二维数组
`16:14` 然后我进行了一些操作 然后又变换回来 我就得到了一张清晰的图像
`16:19` 再一次 这只是为了直观地展示我们所做的
`16:25` 其实是对二维数组的运算 对于每个点 会有对应的值
`16:30` 这个值可以表示特定位置、像素的亮度或者颜色
`16:36` 现在我们知道了图像
`16:40` 在计算机中是如何表示的
`16:42` 现在 我们发现如果我们想要很多像素
`16:47` 想有很多的灰度值 就需要占据很大的空间
`16:52` 因此 在下周的视频中 我们就来讨论压缩的问题
`16:57` 怎样才能在保持图像质量的前提下处理大量的数据
`17:03` 谢谢大家
//果壳教育无边界字幕组  showonder | 风过成伤 | 小白_远游