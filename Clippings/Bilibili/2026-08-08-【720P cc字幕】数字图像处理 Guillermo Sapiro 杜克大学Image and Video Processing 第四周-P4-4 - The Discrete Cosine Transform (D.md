---
title: "【720P cc字幕】数字图像处理 Guillermo Sapiro 杜克大学Image and Video Processing 第四周"
url: "https://www.bilibili.com/video/BV1DE411J7ab/?p=4"
bvid: "BV1DE411J7ab"
cid: "158237943"
author: "筷_拨"
upload_date: "2020-03-01"
subtitle_lang: "中文（中国）"
created: "2026-08-08"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=92679365&bvid=BV1DE411J7ab&cid=158237943&page=4&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

https://www.coursera.org/learn/image-processing/home/week/2
课程名称:Image and video processing: From Mars to Hollywood with a stop at the hospital
教授:Guillermo Sapiro 
教材:冈萨雷斯第三版
来源:https://www.coursera.org/learn/image-processing/lecture/CC

## 字幕

`00:00` 现在对应 YCbCr 的每个域 我们都有 n x n 的方块
`00:06` 现在我们进入正变换这一节 首先
`00:13` 我们需要了解为什么我们要做正变换
`00:17` 首先解释一下 在有损压缩时
`00:24` 我们是如何量度压缩图像所产生的误差
`00:31` 基本想法是 误差是由均方误差 (mean square error)
`00:37` 简称MSE 来量度的 均方误差是这样计算的
`00:46` 均方误差如下
`00:49` MSE=1/像素数目 这只是归一化
`01:00` 我们计算出每个像素上的误差的平方 并将所有的这些值相加
`01:10` 我们得到了重建的图像
`01:13` 将其记为ḟ 用它减去原图像 再将差平方
`01:19` 我们对每个像素计算
`01:24` 现在我们计算 假定原来的 f是100
`01:29` 重建后是98 差值是2 平方后 我们得到4
`01:35` 我们对每个像素做此操作 全部加起来 然后用像素总数做归一化处理
`01:40` 大部分时候 因为我们用了平方
`01:46` 我们会取平方根 所以它被称为均方根误差
`01:52` 这就是我们测量误差的方式 那么 为什么我们需要做变换呢
`01:57` 想象一下 我们取 n x n 的方块
`02:02` 通常 我们取 8行8列
`02:06` 但现在我们讲 n x n 的普遍情况 我们有 n x n 的方块
`02:10` 因为我们将对每个方块独立操作
`02:15` 尽管有 n x n (n=8时 是64) 个像素
`02:19` 我只对其中一个进行传输 我允许你只传输其中一个
`02:23` 那么 如果我们取一幅普通的图像
`02:28` 但只传输其中一个像素 误差将会非常大
`02:32` 但是 我们或许可以做一个变换 将其转为一个不同的 8行8列 的方块
`02:39` 这个变换需要是可逆的 必须可以恢复原状
`02:42` 我们做了这个变换 得到一个新的 8行8列 的方块 或许在变换域
`02:48` 我们可以只取第一个元素 并且取得不太差的效果
`02:53` 事实上 存在一个变换能有这效果
`02:58` 它是卡洛南-洛伊变换 (Karhunen-Loeve Transform) 即 K-L变换
`03:10` 这个变换有一些很特别的地方 看这个变换域
`03:13` 我会马上写出来 变换是什么意思
`03:18` 但是想想看 其实就是乘以一个矩阵
`03:22` 就是那样的操作 我有一个 8行8列 模块
`03:26` 做了一些操作 我得到一个新的 8行8列 模块 也就是 8行8列 的图像
`03:31` 那么假如我刚才做的是 K-L变换
`03:34` 有时它也被写成 KLT 即卡洛南-洛伊变换
`03:39` 假如在变换域中
`03:41` 我取64个元素中的第一个
`03:47` 然后计算均方误差 记住我刚才只用了一个元素去做重建
`03:52` 我拿一幅图像 做 K-L变换
`03:56` 我去掉了其中的63个系数 然后反变换 并且计算均方误差
`04:02` 我取得了在只用一个系数的情况下 可能的最小均方误差
`04:08` 只传输一个元素
`04:12` 如果我想要3个元素 我可以做完全一样的事
`04:15` 而 K-L变换 有趣的地方在于
`04:21` 你不仅仅得到了在只用一个元素情况下最小的误差 而且那个元素是第一个
`04:26` 所以可以想象 有64个元素 你会得到新的64个元素 取第一个
`04:32` 那是你只能用一个元素的情况下 你能得到的最小误差
`04:37` 如果你可以用三个系数 那么就取前三个即可
`04:41` 所以这个变换很好 但是它有一个大问题
`04:46` 这个变换是依赖于图像的
`04:50` 我一开始并不知道怎么操作 正如我刚才所说 这是一个矩阵乘法
`04:54` 但是在我看到图像之前 我不知道要乘的矩阵的系数
`04:57` 你先给我图像 或者关于图像的信息
`05:02` 然后我才可以计算这个矩阵的系数
`05:05` 这样实用性不强
`05:09` 这样很慢 而且 我不能一拿到图像就马上操作
`05:12` 为了替代 我们采用一种
`05:16` 离散余弦变换 (discrete cosine transform) 这种变换是真正在 JPEG 中使用的
`05:20` 我们现在就写下离散余弦变换的公式
`05:26` 我们接下来进一步解释下这个变换
`05:29` 但是要记住 我们其实是想要做 K-L变换
`05:34` K-L变换所做的事情是 消除图像元素之间的相关性
`05:39` 它把很多信息都放进了第一个系数
`05:42` 然后更多一点的信息放进了第二个系数 两个系数间是相互独立的
`05:47` 之后的系数以此类推 而且我们计算得到的均方误差是最优的
`05:52` 但是用离散余弦变换 我们将会得到次优的结果
`05:56` 但这个变换有一个固定的矩阵
`06:01` 有固定的系数 所以是通用的 我们可以对任意图片使用
`06:05` 而不需要做额外计算 那么究竟什么是变换
`06:10` 想法很简单 我们有一张图像
`06:17` 记之为 f(x,y) 来写下这个变换
`06:21` 我们会进入一个新的变换域 T(u,v)
`06:26` 你们中的一些人是熟悉傅里叶变换的
`06:30` 傅里叶变换就是一种变换 我们会将图像中所有的像素相加
`06:36` 记住 我们有n个元素
`06:39` 所以我们将从 0 操作到 n-1 这里也是从 0 操作到 n-1
`06:48` 用图像 f(x,y) 乘以变换系数
`07:01` 这是我们将得到的 我必须给你
`07:08` 关于离散余弦变换 (DCT) 系数的公式
`07:13` 对于K-L变换 这些系数完全依赖于图像
`07:18` 不仅依赖于所取的位置
`07:24` 还依赖于你图像上实际的灰度值
`07:28` 这就是一个变换 我在 x和y 方向求和
`07:34` 那意味着 求和之后 x和y 都将消失
`07:38` 然后我得到一个新图像 是 8行8列 或者 n x n 这是 n x n 的
`07:45` 这是在变换域中 然后 你可以写成矩阵形式
`07:49` 当然 一个变换只有在可逆的时候才有用
`07:54` 如果你给我一幅图
`07:58` 我进入变换域 但是视觉上我却看不懂 这就造成了不便
`08:03` 所以还有一个逆变换公式
`08:07` 这样我可以回到f(x,y) 这将会是从u=0到n-1求和
`08:14` 以及 v=0到n-1 求和
`08:22` 变换系数 T(u,v)
`08:26` 然后是 逆变换 有时会等于 r
`08:34` 有时候不会 这里是 s(x,y,u,v)
`08:40` 如果我拿一幅图 进行这种操作 在变换域里得到一幅新图
`08:47` 然后继续 进行反变换
`08:51` 为了说清什么是变换 我需要告诉你这些是什么
`08:57` r是什么？s是什么？ 再次说明 不难看出
`09:02` 你可以把这些写成矩阵形式 作为自选的作业
`09:07` 这一点也不难 现在我需要说明这两个是什么
`09:13` 这两个函数 r和s 理想化情况下会使均方误差极小
`09:19` 我们希望它是 K-L变换 但做不到
`09:26` 所以会使用DCT 接下来我会写出来
`09:30` DCT是什么？在DCT中 r(x,y,u,v)
`09:40` 实际上等于 s(x,y,u,v)
`09:50` 这个实际上等于 归一化系数 α(u) × α(v) 这两个v是一样的
`10:02` 抱歉我写了两个不一样的v
`10:08` cos[(2x + 1) u π / (2n)]
`10:15` 这是大小 乘上 完全一样但是是另外一个坐标
`10:37` cos[(2y + 1) v π / (2n)]
`10:45` 这是通用公式 现在我将写下来
`10:51` 这个是什么 这是归一化 在这类变换中很常见的
`10:56` 当u=0时 对(1/n)求平方根
`11:08` 当 u不为0时 对(2/n)求平方根
`11:11` 所以 我刚给出了DCT 即离散余弦变换的公式
`11:16` 对于熟悉傅里叶变换的人来说
`11:21` 这基本就是傅里叶变换的实部
`11:27` 几乎就是那个部分
`11:30` 再说一次 正变换和逆变换在这里是一样的公式
`11:35` 很好
`11:36` 当你需要用软件或者硬件实现
`11:42` 这些正变换和逆变换的时候 只要实现余弦变换就行了
`11:47` 所以这是余弦 这也是余弦
`11:49` 然后这些是归一化系数
`11:53` 这就是离散余弦变换
`11:58` 我想要告诉你这些函数
`12:02` 这些是我们在变换中 将用来和图像相乘的矩阵
`12:08` 正如之前所见
`12:13` 这个例子中 为了使演示简单一些
`12:21` 我们取n=4 对于每一个基函数
`12:26` 如果我用公式表达 当 u 和 v 都为 0 时 我得到一个余弦函数
`12:34` 这是一个仅关于x和y的函数 这些公式之前是关于u、v、x、y的函数
`12:41` 但现在我固定u和v
`12:43` 让 u和v 均为0 我得到一个只有 x和y 的公式
`12:48` 这是我得到的 4 x 4 图像
`12:53` 如果我让 v=1 且 u=0 我得到这个 以此类推
`12:59` 所以实际上 r和s 就是我们说的 基函数
`13:06` 而我们是在试着将 n×n 大小的小图像
`13:14` 分解成这些基函数的线性组合
`13:19` 如果是纯色图像 这里的T(u,v) 只要 u和v 的值都为0即可
`13:27` 如果图像中变化频繁
`13:34` 那么你就需要一些系数 像 T(3,3) 这样  每一个 T(u,v)
`13:42` 都会描述你有多少这样的组成部分
`13:50` 现在我们已经用此类图像的线性组合
`13:53` 来表示 n × n 的图像了
`13:58` 很重要的一点是 这些图像是不变的
`14:02` 而我们提到过 在卡洛南-洛伊变换中它们并非如此
`14:09` 因此很难有高效的硬件实现可以实时完成那种变换
`14:14` 而此处的基图像是固定不变的 你给我一张图
`14:19` 我就会用这些基图像的一种线性组合来表示它
`14:23` T(u, v) 也告诉我们 我们的图像里 要乘以的 n × n 图像里
`14:31` 有多少这些基图像 这就是变换
`14:34` 实际上这些系数最能告诉我们这些基图像
`14:40` 都包含了多少信息 我把图像
`14:45` 分解成这些分量 这些属于频率成分
`14:49` 可以看到 当u和v分别沿纵向和横向增大时
`14:55` 图像的变化更多了 有一点我之前没有告诉大家
`15:01` 我说过我们不能做卡洛南-洛伊变换 对于 K-L变换
`15:06` 计算其基函数太过繁复 因此我们进行离散余弦变换
`15:11` 为什么选择离散余弦变换？
`15:14` 为什么不选择傅里叶变换或其他变换方法 譬如哈达玛变换 (Hadamard Transform)？
`15:19` 变换方法有很多种 我们选择离散余弦变换有几个理由
`15:24` 其一是 虽然我想进行卡洛南-洛伊变换
`15:27` 我需要做离散余弦变换
`15:31` 其实在一些特定情况下 离散余弦变换
`15:36` 完全等同于卡洛南-洛伊变换 这些特例中的图像
`15:41` 其像素排列符合马尔可夫链 即每个像素都以一种特殊的形式
`15:47` 依赖于相邻像素 然后相邻像素也是如此
`15:52` 以此类推 这就是所谓的一阶马尔可夫图像源
`15:56` 即 Markovian 如果可以设定一张图像
`16:04` 就可以证明卡洛南-洛伊变换最终就是离散余弦变换
`16:10` 所以最终 像我们这里看到的一样
`16:14` 它的基图像是固定不变的 这就是我们使用离散余弦变换的一个原因
`16:19` 另一个我们使用离散余弦变换的
`16:24` 原因是这样的 我们是从整个图像着手操作的
`16:32` 然后是这样的 n × n 的小块
`16:40` 有人可能会疑惑为什么不用傅里叶变换之类的方法呢？
`16:45` 如果回去看傅里叶变换的理论 当我们进行离散傅里叶变换时
`16:52` 我们对周期性有一个潜在的假设
`16:58` 我们是假设图像像我们看到的一样在自我重复
`17:05` 这一点在此处再次出现 而这一点则在这儿重复
`17:09` 所以这里图像在反复出现
`17:14` 有了这个潜在的数学上的假设
`17:19` 我们才能真正地对 8 × 8 的小块进行傅里叶变换 在此 我只是标出了
`17:25` 8 × 8 的小块中的一条线 但这个假设同样适用于每个方向
`17:32` 以及整个二维平面 也就是说我们处理完一个小块之后
`17:38` 我们需要下一个小块中的像素
`17:44` 与这个小块的这个像素完全相同 即是说我们需要这个小块
`17:50` 在这里和这里重复 这就是你的潜在的假设
`17:56` 这里和这里的像素一样的可能性很低
`18:03` 就算是在 8 × 8 小块中 这两个值可能也不接近
`18:08` 另一方面 离散余弦变换对周期性所作的假设是不同的
`18:15` 离散余弦变换假设边界处有镜面对称
`18:21` 即是说你的图像是在重复
`18:24` 但它可以说是翻折了 这个假设是说
`18:29` 假定这里的像素与相邻的这个像素类似
`18:35` 不是说像素与八个像素之后的那一个相同
`18:41` 而是与紧接着它的那个像素相同 这个假设更合理
`18:47` 这就是我们选用离散余弦变换的两个原因
`18:51` 其一是对一类图像而言 进行离散余弦变换就是在进行卡洛南-洛伊变换
`18:56` 其二是周期性 在我们从 n × n 或 8 × 8 小块开始处理图像时
`19:03` 离散余弦变换对周期性的假设更为实用
`19:10` 现在我们知道离散余弦变换了 我们从一幅图像开始
`19:14` 将它分为 n × n 的小块 我们进行离散余弦变换
`19:19` 所得的系数表明我们用到了多少基图像
`19:25` 我一直在讲 “8 × 8” 我们怎么得到 8 × 8 这个值的？
`19:30` 其实 JEPG 是在大量研究之后被设定为 8 × 8 规格的
`19:36` 我就解释一下这是怎么回事
`19:40` 这张图 我们取了整个图像
`19:44` 即是说我们没有做分割 这已经是我们提到过的
`19:49` Lena的图像的一块了 我们对整幅图
`19:54` 进行离散余弦变换 并取系数的25%
`19:59` 也就是说我们只取了四分之一的系数 并将其它系数设为零
`20:06` 之后我们进行逆变换 这是我们得到的结果
`20:11` 我们介绍过误差 记住离散余弦变换所作的是
`20:16` 尽可能把我们引入一个变换域 这会产生误差
`20:21` 因为在其中会去掉一些系数 将它们设为 0
`20:25` 或引入一些差值 但只会造成很不显眼的误差 但这里我们做得很简单
`20:31` 我们只取了 25%的离散余弦变换系数 我们很快会知道所取的系数是哪些
`20:36` 但请先想象我们取了 1/4的值最大的那些系数
`20:41` 首先我们是对整张图做处理
`20:46` 这里我们只取 2 × 2 的小块 就是说我们这里把此图分割为 2 × 2 的小块
`20:53` 我们对每个 2 × 2 小块进行离散余弦变换 然后取系数的25%
`20:58` 所以我们最终只用了一个系数 然后逆变换
`21:02` 可以看到这个非常恼人的块效应
`21:06` 我们对 4 × 4 的分块方式 以及 8 × 8 的分块方式 分别做了同样的处理
`21:13` 我们看到图像效果越来越好
`21:17` 也许大家会想 2 × 2 是太小了
`21:21` 但为什么我们停在 8 × 8 ？ 为什么不选择 16 × 16 或 32 × 32 ？
`21:26` 或者为什么不对整幅图做离散余弦变换？
`21:31` 有这样几个原因 其一是计算方面的
`21:34` 进行多个小型离散余弦变换比一个大型变换更划算
`21:41` 所以如果你有一个 16 × 16 的图像 对四个 8 × 8 小图进行离散余弦变换
`21:50` 比对一个 16 × 16 大图做变换更方便 这是第一个原因
`21:56` 另一个原因是离散余弦变换与卡洛南-洛伊变换奇妙的相似性
`22:00` 我说过这发生在特定条件下
`22:04` 即为一阶马尔可夫图像源时
`22:07` 如果你是在 8 × 8 小块里的一个小范围内
`22:12` 马尔可夫链条件可能可以成立
`22:17` 但如果你在一个很大的图像上操作 整张图不会满足马尔可夫链条件
`22:23` 就是说 小范围中假设可能成立
`22:26` 但在很大范围中则不行
`22:32` 这并不难理解 譬如说 你现在正看着我
`22:35` 你会看到这里有很多相关性 灰度值几乎一样
`22:41` 但如果向其它地方看
`22:44` 这里和这里的像素离得这么远
`22:50` 这两个像素值之间有相关性吗？其实是没有的
`22:54` 我可以换一件衬衫 而不需要考虑之前的信息
`23:00` 当你图中的像素离得很远的时候 其实它们就没有相关性了
`23:05` 它们就是不相关的 所以你需要折中选择
`23:11` 一方面你希望有很小的小块以实现高效计算
`23:16` 另一方面你不希望小块太小
`23:21` 因为你会遇到这样的问题 小块中没有足够的信息
`23:26` 没有足够的关联性 在变换中会被去相关
`23:31` 如果你选了太大的模块
`23:35` 你将要面对低效的计算 并且你忽视了 DCT 的优点的设定条件
`23:41` 你将为此付出代价 所以 8 × 8 是个不错的选择
`23:46` 我认为这是深入研究后很有意义的成果 它是很好的折中办法
`23:51` 这就是它如今被用在 JPEG 上的原因 作为对本部分离散余弦变换的介绍的总结
`23:57` 我想要再展示一次 Lena 的图像
`24:03` 这是两个不同的例子 现在先不要担心它
`24:08` 这里 我们取用 8 × 8 模块 进行离散余弦变换 我们保留 12.5%的系数
`24:15` 然后我们进行逆变换 没有真正进行量化 那是我们下一步的内容
`24:21` 这里我们只进行离散余弦变换和它的逆变换
`24:25` 这看起来很不错 就是说 我们第一眼看到它时
`24:31` 我们已经成功压缩了 因为我们
`24:37` 只保留了像素的12.5% 但得到了看起来和原图几乎一样的效果
`24:43` 这是因为我们是在离散余弦变换的变换域中进行处理的
`24:47` 如果我们只是扔掉大量像素 只保留它的12.5%
`24:53` 我们不能得到这样质量的图像
`24:57` 这是原图和重建的图像之间的误差
`25:01` 可以看到 这是个很小的误差
`25:05` 所以说单单一个离散余弦变换
`25:10` 去除大量系数 然后逆变换 就已经完成了相当不错的压缩
`25:14` 现在我们已经了解了前端和后端
`25:18` 只差中间部分 我现在在 DCT域中
`25:24` 我能通过量化处理高明地实现更大程度的压缩么？
`25:28` 这将是我们下一个视频的主题
// 果壳教育无边界字幕组 | 和风煦日 | leafsherry | Coowesome | wugui | 小白_远游