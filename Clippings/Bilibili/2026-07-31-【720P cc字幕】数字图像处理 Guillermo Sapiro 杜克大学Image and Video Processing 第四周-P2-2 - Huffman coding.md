---
title: "【720P cc字幕】数字图像处理 Guillermo Sapiro 杜克大学Image and Video Processing 第四周"
url: "https://www.bilibili.com/video/BV1DE411J7ab/?p=2"
bvid: "BV1DE411J7ab"
cid: "158236904"
author: "筷_拨"
upload_date: "2020-03-01"
subtitle_lang: "中文（中国）"
created: "2026-07-31"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=92679365&bvid=BV1DE411J7ab&cid=158236904&page=2&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

https://www.coursera.org/learn/image-processing/home/week/2
课程名称:Image and video processing: From Mars to Hollywood with a stop at the hospital
教授:Guillermo Sapiro 
教材:冈萨雷斯第三版
来源:https://www.coursera.org/learn/image-processing/lecture/CC

## 字幕

`00:00` 我们现在要讲解建立图像压缩算法的每一个环节
`00:04` 而且会特别详细讲解静态图像压缩标准 (JPEG)
`00:07` 之前也提到过 我们将从符号编码器讲起
`00:11` 我们会对图像数据做变换和量化 这些环节在后续课程会详细讲解
`00:16` 在完成所有变换和量化之后 我们希望进一步压缩图像
`00:21` 量化会提供一定程度的压缩 但我们仍希望进一步压缩
`00:25` 我们需要存储量化后的数据
`00:28` 我们需要保证精确无误地存储
`00:31` 但是也需要思考如何进行压缩
`00:34` 那么 为什么能做到这些呢?  其基本思想很简单
`00:38` 当然 如果不至少先给大家看一次这张叫 Lena 的非常著名的图片
`00:44` 那就不算是教过图像处理
`00:47` 在图像处理中 经常用到这张图片
`00:51` 但现如今 由于我们拥有丰富的图像资源 也会使用许多其它图像
`00:56` 不过我还是想在这九周中至少给大家展示一次这张图片
`01:00` 而且在以后的课程中我们可能还会多次见到它
`01:04` 基本思路是这样的 图像中的一些像素 一些灰度值出现的次数多于其它灰度值
`01:10` 我们来看看这个现象
`01:13` 在右边 有一幅直方图
`01:16` 直方图记录每一特定的灰度值在图像中出现的次数
`01:22` 比如说 某一像素的灰度值
`01:25` 假设灰度值大约是47 它出现了这么多次
`01:30` 我们遍历整幅图像 计算有多少灰度值为47的像素点
`01:35` 这就是它出现的次数 我们继续看下一个灰度值 比如说200
`01:39` 我们看到 灰度值200在这幅图中大约出现了700次
`01:44` 我们可以看到有些像素值出现了许多次
`01:49` 而另一些像素值 比如 0或1 根本就没有出现过
`01:52` 因此 我们要采用的方法的基本思路是
`01:57` 给出现次数很多的像素值一个非常简短的编码
`02:02` 而给出现次数较少的像素值很长的 或者说相对较长的编码
`02:07` 下面来看看具体如何编码
`02:10` 下面来看一个应用上述方法的例子 这里是一个图像
`02:16` 这正是大家在前面视频中见过的那个几何图像
`02:20` 这是比较特殊的图像 它只有四个不同的灰度值
`02:25` 而灰度值为87的像素占了整个图像的四分之一
`02:32` 即 25% 或者说 灰度值47 出现的概率是 0.25
`02:37` 同时灰度值为128的像素几乎占了图像的一半
`02:44` 这里是灰度值186 而 255 出现的次数极少
`02:50` 其它的灰度值并没有出现 所以表示这个图像的标准方式是
`02:55` 用到了数组 对于每一个像素 使用八位二进制码表示其像素值
`03:01` 所以 我们用87的二进制码来表示灰度值87
`03:04` 用128的二进制码来表示灰度值128
`03:10` 以此类推 所以在最后 如果我们不进行压缩
`03:15` 那每个像素要占 8比特
`03:19` 但是现在来假设一下 如果我想办法构造了如图所示的编码
`03:27` 其中 87是用二进制码01来表示的
`03:31` 128 用二进制码1 来表示
`03:37` 186用二进制码000表示 而255用二进制码001
`03:42` 这就意味着 比如说
`03:47` 如果想要存储或发送 87、87、255、128
`03:52` 这几个像素值
`03:55` 并不是用这些二进制码 然后是这个 而是用这个表示255 这些来表示128
`04:02` 这种表示方式需要 4乘以8 也就是32比特位 不如我们用以下这种方式来表示
`04:10` 我会假设
`04:13` 87用01来表示 87还用01来表示
`04:18` 255用001来表示
`04:21` 128用1来表示 所以这里需要2 4 7
`04:27` 一共8比特 而不是32比特 来表示同一组灰度值
`04:35` 现在 解码器会根据这个表格解码 所以解码器看到01时
`04:42` 会在这个像素的位置输出 87 看到01 再次输出 87
`04:48` 然后发现 001 这里要输出255 等等
`04:53` 如此就完成了压缩 现在来看看这到底压缩了多少
`04:59` 我们来做一个非常简单的计算
`05:02` 我们来看一看
`05:06` 抱歉
`05:10` 我们有0.25的概率
`05:22` 需要用到长度为2的二进制码01
`05:27` 有0.47的概率 使用长度是1的二进制码 这里加上0.47乘以1
`05:36` 有0.25的概率 也就是四分之一的图像中 我们要用到长度为3的二进制码
`05:45` 最后是很小的概率
`05:55` 同样用到长度为3的二进制码 结果等于1.81
`06:04` 好了
`06:10` 所以现在整个图像中平均每个像素只占1.81比特 而不是之前的8比特
`06:18` 这里省了很多像素 从8减到了1.81
`06:26` 也就是说 压缩了四倍以上
`06:29` 这就是我们刚刚做的
`06:32` 刚刚我们利用了一些像素值的出现次数远比另一些多的现象
`06:37` 然后用更短的编码来表示这些像素值
`06:43` 这里用了更短的编码 那么 怎么才能做到这一点呢
`06:47` 这里我们用到了霍夫曼编码 接下来我会解释怎么运用这种编码
`06:51` 再看看这个例子
`06:55` 解释霍夫曼编码最好的办法是举个例子
`07:01` 接下来我要用这个例子说明 现在假设我们用五个不同的符号
`07:06` 抱歉 是六个不同的符号
`07:09` 第一步 按概率大小排列这些符号
`07:15` 概率从高到低
`07:19` 如果有些概率相同
`07:24` 那么无所谓谁先谁后 比如说 a2可能代表像素值100
`07:30` 符号 a6 可能代表像素值77 等等
`07:34` 现在我会做一些加法
`07:37` 先把符号列在这里 把概率列在这里
`07:42` 然后从下往上 取最底下的两个数
`07:47` 把它们的概率相加 用0.04加上0.06
`07:53` 得到了0.1的概率 然后将这一列概率重新排列一遍
`07:58` 这些都没有变 唯一改变了的是最后一个
`08:04` 看看这里的变化 0.4的概率对应于a2
`08:09` 0.3的概率指向a6 0.1的概率指向a1
`08:16` 0.1的概率指向 符号 a4
`08:20` 而最后一个概率0.1对应a3或a5
`08:25` 我们要用一些办法来区分 a3和a5
`08:31` 下一张幻灯片会解释如何区分它们
`08:36` 现在 重复一下刚才的步骤
`08:38` 将最低的两个值相加
`08:42` 现在 这个值是0.2 记住每次都要重新排序
`08:47` 让我再解释一下 概率0.4对应a2
`08:52` 0.3对应a6 而现在 0.2对应这两个概率值
`08:58` 也就是 a4、a3还有a5
`09:00` 我们现在还不能确定是哪个符号 但是0.2的概率对应着其中一个
`09:07` 然后这里是0.1 重复刚才的步骤 再重复一次
`09:12` 实际上 还需要重复两次 我们得到这两个最小值
`09:16` 算出这个值 再把它和最小的值相加 然后得到这个值
`09:21` 继续重复 每一次我们都将最小的两个值相加得到下一列概率值
`09:26` 并且重新排序 直到只剩下两个数
`09:32` 然后来看看为什么在这里停下 下面我们来进行编码
`09:37` 这是依据前一幻灯片中图表的一个重绘
`09:46` 这是第一列
`09:51` 这是第二列 第三列 第四列 和第五列
`09:57` 现在 从后往前 依次赋值编码
`10:01` 给0.6赋上0 给0.4赋上1
`10:05` 在继续往回推进之前 我来解释一下这里是怎么回事
`10:10` 当解码器看到0 它会明白这是从 0.6对应的这一组来的
`10:15` 当它看到一个 1
`10:20` 就会知道是 0.4对应的这一组
`10:24` 然后要添加更多的比特位来区分每组中的各个数值
`10:30` 这就是为什么要从后往前走 把这个 0 抄送到这里来
`10:36` 这个0.6是由 0.3加0.3 得来的
`10:42` 所以要在后面分别加上0和1 当然我们要把 0.4抄送到这里
`10:49` 给它赋上从上一列得来的码值 这里不用加任何数字
`10:56` 所以它会一直不加任何比特位被抄送到前面
`11:00` 现在 要找到与这组对应的所有符号
`11:04` 而这里 我们会看到 其实只有一个符号对应于这个组
`11:09` 现在有了这个编码和所有指向这个概率的符号
`11:16` 这里的编码是从 01 开始的
`11:22` 现在继续往前抄送 将 1 抄送到这里 为1
`11:29` 00 抄送为00 而01需要被分开因为这个0.3的概率
`11:33` 是由这两个相加得到的
`11:38` 接着就变成了010和011 然后继续
`11:43` 把1抄送过来 0抄送过来 这个011的编码抄送到这里
`11:50` 而这个要分开
`11:56` 每次之所以要把它分开 是因为之前在算它的时候是相加得到的
`12:02` 然后回到这里 这里要分开
`12:05` 每次分开的时候 都要加上0和1
`12:10` 所以一直继续 就得到了这个结果
`12:17` 也就是第一个符号的编码是1 符号a6的编码是00 以此类推
`12:23` 所以这就是它们的编码
`12:25` 这些是变长编码 用来表示各个符号的编码长度并不完全一样
`12:30` 各个编码是有不同长度的
`12:35` 而这个长度取决于概率 概率越大 编码越短
`12:40` 这很容易看出
`12:44` 因为低概率值在不断被相加
`12:50` 当往回推导的时候 就要不断分开
`12:53` 记住 每次分开的时候都要加一个符号 因此要为这个符号加一个比特位
`12:58` 所以 编码变得越来越长
`13:03` 这里也可以算出编码的平均长度
`13:07` 回顾一下之前的步骤
`13:12` 按照概率排列符号的顺序
`13:16` 这是这一步做的 然后开始把低概率相加
`13:20` 将这两个相加 就得到了这个 重新排列然后继续
`13:25` 这是一个递归的过程 所以如果知道了怎么从一列进行到下一列
`13:31` 就知道了怎么完成它 然后回来看这个
`13:36` 这里有几件非常重要的事情要注意
`13:41` 用这种方法得到的编码叫做无前缀编码
`13:47` 这是非常 非常重要的 来看这一列
`13:51` 当这里有一个1 解码器看到一个1 它就知道要到这里找到a2
`13:59` 当这里有一个0 这就是在说
`14:03` 好吧 我要等着看下一个符号是什么
`14:10` 但是没有任何一段码字完全是其它编码的前缀 这是非常重要的
`14:16` 不然解码器是无法重建图像的 看看下面这个例子
`14:22` 这里的码字会是前缀 比如 符号a1是用 码字1表示的
`14:27` 符号 a2 是用 码字0 表示的
`14:33` 符号a3 是用 码字01 表示的
`14:37` 当解码器看到01
`14:44` 会出现两种可能 一种可能是a3
`14:49` 假设它对应的像素值是100 另一种可能是 a2后面跟着a1
`14:57` 假设像素是0和255 因此重建过程就出现了很大的歧义
`15:04` 无法分辨到底是哪一个
`15:09` 因为这个编码并不是无前缀编码
`15:15` 这个是这个的子集 而在这里就不会发生这种情况
`15:21` 现在把这些注释都清除
`15:27` 这里没有任何一个编码有所重复 比如说00
`15:33` 因此 当解码器看到了一个1 就知道怎么办
`15:38` 它会找到a2的像素值 当它看到一个0
`15:43` 它需要继续分辨 因为这里很多码字都是以 0 开头的
`15:49` 如果它看到有一个0 之后跟着一个0
`15:53` 它会完成这段码字的解码 并得到a6
`16:00` 如果解码器收到了一个0 然后跟着一个1 它还需要继续分辨
`16:07` 因为有很多码字都是以01开头的 但是之后 如果又接收到了一个1
`16:11` 它就完成了这段解码 不需要继续分辨了
`16:14` 因为这里什么都没有 没有歧义
`16:18` 这是非常重要的 霍夫曼码在构建时
`16:22` 我们这样一次又一次地进行分裂
`16:25` 它的构建过程 就决定了它是无前缀编码
`16:29` 那么 如何得知它是最优编码方法呢？
`16:33` 如何得知利用这组概率
`16:37` 以及我刚刚解释的那个过程
`16:43` 就能达到最短的编码平均长度呢？
`16:48` 这个定理有待证明
`16:52` 在这节课上我们不会涉及到这个证明 但是有一个定理
`16:57` 能够证明霍夫曼编码可以达到最短的长度
`17:02` 现在 你可能会好奇霍夫曼编码 或者其它类似的编码器
`17:07` 能达到的长度是多少 所以基本的思想是
`17:12` 当面对一组概率时 能够在多大程度上对其进行压缩？
`17:15` 在构建霍夫曼码之前 我们想要知道
`17:21` 这值不值得我们花费精力去构建这种编码？ 可以压缩很多吗？
`17:25` 而这就要通过计算熵来得到答案
`17:30` 熵使用符号 H 来表示
`17:33` H 就是指熵
`17:36` 熵是 所有符号的概率乘以该概率以2为底的对数 对所有乘积求和 再取负数
`17:55` 关于这个负号 我等一下会解释
`18:05` 所以在这里就是 0.4 乘以 0.4以2为底的对数
`18:12` 加上0.3乘以 0.3以2为底的对数 依次类推
`18:18` 所以实际上最终得到的是一个正数
`18:24` 因为所有概率都是小于1的 所以概率的对数是负数
`18:30` 加上这个负号 就会得到正数
`18:34` 香农第一定理就是在说
`18:40` 我们可以渐渐逼近这个平均编码长度
`18:45` 也就是像霍夫曼编码这类编码方式所能达到的 (最短长度)
`18:51` 可以来算算这个数
`18:56` 从而得到编码的平均长度
`19:00` 也就是理想的平均码长
`19:05` 实际上这个公式并不难理解
`19:10` 之前的例子用过类似的计算
`19:14` 这个概率就是符号出现的概率
`19:19` 而这是这个符号对应的编码长度
`19:23` 这就是说 理想的编码长度就是这个概率的对数
`19:30` 之所以是理想值是因为
`19:36` 编码长度不能是3.2 而只能发送完整的比特数
`19:42` 或者发送 3比特 或者发送 4比特
`19:46` 这就是为什么 它是平均期望值
`19:51` 也即是编码长度的理想值
`19:57` 但是这能帮助你理解霍夫曼编码的编码能力
`20:01` 现在大家已经知道如何用比特进行编码
`20:07` 我们要开始进入图像压缩的下一个模块了
//果壳教育无边界字幕组 | 冰之冷 | 某音某莹 | 林小七Lynn | 小白_远游