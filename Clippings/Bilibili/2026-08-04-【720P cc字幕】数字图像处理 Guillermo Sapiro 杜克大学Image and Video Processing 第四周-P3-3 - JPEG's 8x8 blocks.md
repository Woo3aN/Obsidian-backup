---
title: "【720P cc字幕】数字图像处理 Guillermo Sapiro 杜克大学Image and Video Processing 第四周"
url: "https://www.bilibili.com/video/BV1DE411J7ab/?p=3"
bvid: "BV1DE411J7ab"
cid: "158237671"
author: "筷_拨"
upload_date: "2020-03-01"
subtitle_lang: "中文（中国）"
created: "2026-08-04"
tags: ["clippings", "bilibili"]
---

<iframe src="https://player.bilibili.com/player.html?aid=92679365&bvid=BV1DE411J7ab&cid=158237671&page=3&autoplay=0" scrolling="no" border="0" frameborder="no" framespacing="0" allow="fullscreen; picture-in-picture" allowfullscreen="true" style="height:100%;width:100%; aspect-ratio: 16 / 9;"> </iframe>

## 简介

https://www.coursera.org/learn/image-processing/home/week/2
课程名称:Image and video processing: From Mars to Hollywood with a stop at the hospital
教授:Guillermo Sapiro 
教材:冈萨雷斯第三版
来源:https://www.coursera.org/learn/image-processing/lecture/CC

## 字幕

`00:00` 通过一个例子 我们现在已经完成了基于霍夫曼编码的简单编码
`00:06` 这是图像压缩的后端步骤 接下来让我们看看图像压缩的前端
`00:12` 在这个视频中我们将要讲述的是这一部分
`00:17` 那么让我们从这个部分开始
`00:23` 现在我们有一张图片 一个粗略的想法是 JPEG
`00:30` 或者有损变换编解码压缩 做的是把图片分成一些小块
`00:38` 每一个小块有 n x n 个像素点 我们要讲的是
`00:45` 用一个例子来讲解 n 值是什么 但我们先了解一点
`00:50` 就是 JPEG 使用的是 8行8列  的小块
`00:55` 所以 JPEG几乎是将一幅图像中的每个 8行8列 小块独立编码
`00:59` 小块都是不重叠的 所以这是8个像素 接着8个 接着8个
`01:06` 接着8个 好
`01:06` 然后全部都是这样 并且也有方法来处理
`01:11` 如果图片的宽度或者长度不是8的倍数的情况
`01:15` 所以现在不用担心这个
`01:17` 这些只是技术细节 我们取其中一个 n x n 的小块
`01:22` 接下来我们将要对每一个这样的 n x n  8行8列 的小块编码
`01:27` 并且我之前讲过 编码的时候它们几乎是独立的
`01:30` 这就是要对一张图片做的事 但是这只是针对黑白图片
`01:36` 如果是一张RGB图片怎么办呢
`01:41` JPEG 其实是个色盲 JPEG 不知道如何处理色彩
`01:47` 它本身不理解颜色标准 但是我们可以想一下怎么来处理这个问题
`01:53` 最原始的一个想法是 首先把红色通道取出来
`01:56` 因为每一个通道都是一个二维像素阵列
`02:01` 于是红色通道看起来是这样的 我们将红色通道分成 8行8列 的小块
`02:05` 之后对红色通道编码 然后对绿色通道做同样的处理
`02:10` 最后对蓝色通道做同样的处理 看起来这个方法非常合理
`02:14` 但是实际情况是 通道之间有很多相关性
`02:19` 所以 JPEG 做的是
`02:23` 它不对RGB编码 而是处理一种叫YCbCr的色彩空间
`02:28` 接下来我就来告诉大家怎么做这个
`02:32` 但是在这里
`02:35` 我们有Y、Cb和Cr三个通道 而不是RGB三个通道
`02:39` 这个是亮度通道 (luminance channel) 这就是大家
`02:45` 在黑白电视机和黑白显示器上见到的通道
`02:49` 我们有一张彩色图片 但是只能看到黑白的部分
`02:55` 也就是它的亮度部分 这些是色彩通道
`03:00` 好 这是一个简单的转换
`03:04` 实际上是通过乘以一个 3 x 3 的矩阵来实现的
`03:09` 我们把Y Cb Cr列成一列
`03:14` 这里是一个 3 x 3 的矩阵
`03:25` 这是 R G B的值
`03:31` 所以Y是R G B的一个线性组合 Cbr也是一个线性组合
`03:38` Cb是一个线性组合 Cr是另一个线性组合 这个矩阵是可逆的
`03:45` 所以在对YCbCr解码时 将它们乘以这个矩阵的逆矩阵
`03:53` 就能回到R G B值了 JPEG最开始会做的事
`03:57` 是将彩色图片的每一个像素乘上一个矩阵
`04:05` 比如 100、150、200 乘上这里的这些系数
`04:09` 这是一个常数矩阵 这个矩阵非常有名
`04:14` 它在很多地方都有出现 如果用 谷歌 (Google) 搜索 YCbCr
`04:20` 就能够知道这个矩阵的确切数值 这样对图像的每一个像素点都进行处理
`04:25` 把红绿蓝 (RGB) 阵列转换成 YCbCr 阵列
`04:31` 我再重复一次 这仍然是所有像素点的一个二维阵列 不过已经从RGB
`04:38` 转换成了YCbCr的二维矩阵 有了这些 接下来就是
`04:42` 对其中的每一个阵列分别进行编码
`04:46` 让我们来看看Y通道 先处理Y通道
`04:50` 将其分成 8行8列 的小块
`04:54` 好 我们要做的就是这些 接下来把每一个 8行8列 小块取出来
`04:59` 将它们分别编码
`05:03` 如前所述 几乎独立地编码 但是为了更好地讲解它
`05:08` 我们将其认为是独立的
`05:11` 接着要将很多 8行8列的小图片编码
`05:15` 接下来它将怎么做呢 接下来要做的是一个离散余弦变换 (DCT)
`05:19` 一个变换 然后是量化
`05:23` 然后是符号编码 就像我们已经见过很多次的那个 JPEG 流程图表示的那样
`05:27` 现在我们有了这个 8行8列的小块
`05:31` 现在我们需要关注的是我们有的这些 8行8列的图像
`05:37` //果壳教育无边界字幕组 | ctzsm |  leafsherry | 小白_远游